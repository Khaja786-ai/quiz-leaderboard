# quiz-leaderboard

This project is a simple plain-Java workspace with source under `src/com/quiz`.

## Run

Open `src/com/quiz/QuizLeaderboard.java` in VS Code and run it with the Java extension.

If you need to change the registration number, update `REG_NO` in `QuizLeaderboard.java`.
